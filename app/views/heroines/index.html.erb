<h2>All Heroines</h2>
<ul>
  <% @heroines.each do |heroine| %>
    # <li>
      <%= link_to heroine.name, heroine_path(heroine) %>
    # </li>
    <br>
  <% end %>
</ul>
 


<%# On the heroines index page, a heroine's super name should link to that heroine's show page. %>